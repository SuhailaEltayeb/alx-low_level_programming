#include "main.h"

/**
 * add- adds two integers and return result
 * @a: integer to be added to b
 * @b: integer to e added to a
 * Return: summation of a and b in c
*/

int add(int a, int b)
{
	int c;
	c = a +b;
	return (c);
}

#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: first intiger
 * @b: second intiger
 * Return: Always 0 (success)
*/

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
